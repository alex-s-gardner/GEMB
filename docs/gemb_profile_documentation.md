# `gemb_profile` documentation
`gemb_profile` generates a tabulated Profile from a `gemb` output structure that can be used as the input of a new `gemb` run. 

See also [`model_initialize_profile`](model_initialize_profile_documentation.md).

# Syntax 
```matlab
Profile = gemb_profile(OutData)
Profile = gemb_profile(OutData, time_i)
```

# Description

`Profile = gemb_profile(OutData)` creates a `Profile` table containing column properties from the last time step OutData from the `gemb` function. 

`Profile = gemb_profile(OutData, time_i)` specifies a query datetime `time_i` corresponding to the desired output profile. If `time_i` is not specified, the last time step `OutData` is used. If `time_i` does not exactly match any elements in `OutData.time`, the profile corresponding to the nearest time step will be returned. 

# Example 
Run GEMB using simulated climate forcing and the default profile generated by [`model_initialize_profile`](model_initialize_profile_documentation.md). Use `gemb_profile` to extract the profile of the final timestep, then get a profile corresponding to June 15, 2024:

```matlab
% Initialize model parameters:
ModelParam = model_initialize_parameters(output_frequency="daily");

% Generate sample data: 
time_step_hours = 3;
ClimateForcing = simulate_climate_forcing("test_1", time_step_hours);

% Initialize grid:
ProfileIn = model_initialize_profile(ModelParam, ClimateForcing);

% Run GEMB: 
OutData = gemb(ProfileIn, ClimateForcing, ModelParam);

% Get the final profile: 
ProfileOut_end = gemb_profile(OutData); 

% Get a profile corresponding to June 15, 2024:
ProfileOut_june = gemb_profile(OutData, datetime('june 15, 2024')); 
```

Plot all variables in the profile. Include the initial profile generated by `model_initialize_profile`, the final profile of the time series, and the profile corresponding to a specific date: 

```matlab
figure('Position',[50 50 1200 300])
tiledlayout(1, 9, 'TileSpacing', 'compact', 'Padding', 'compact')

nexttile
plot(ProfileIn.dz,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.dz,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.dz,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'dz (m)'
ylabel 'z\_center (m)'
box off
legend('ProfileIn','ProfileOut\_end','ProfileOut\_june', 'location','northwest')
legend boxoff

nexttile
plot(ProfileIn.temperature,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.temperature,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.temperature,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'temperature (K)'
box off
set(gca,'ycolor','none')

nexttile
plot(ProfileIn.density,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.density,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.density,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'density (kg m^{-3})'
box off
set(gca,'ycolor','none')

nexttile
plot(ProfileIn.water,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.water,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.water,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'water (kg m^{-2})'
box off
set(gca,'ycolor','none')

nexttile
plot(ProfileIn.grain_radius,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.grain_radius,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.grain_radius,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'grain\_radius (mm)'
box off
set(gca,'ycolor','none')

nexttile
plot(ProfileIn.grain_dendricity,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.grain_dendricity,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.grain_dendricity,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'grain\_dendricity'
box off
set(gca,'ycolor','none')

nexttile
plot(ProfileIn.grain_sphericity,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.grain_sphericity,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.grain_sphericity,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'grain\_sphericity'
box off
set(gca,'ycolor','none')

nexttile
plot(ProfileIn.albedo,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.albedo,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.albedo,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'albedo'
box off
set(gca,'ycolor','none')

nexttile
plot(ProfileIn.albedo_diffuse,ProfileIn.z_center,'-',linewidth=1)
hold on
plot(ProfileOut_end.albedo_diffuse,ProfileOut_end.z_center,'-',linewidth=1)
plot(ProfileOut_june.albedo_diffuse,ProfileOut_june.z_center,'-',linewidth=1)
xlabel 'albedo\_diffuse'
box off
set(gca,'ycolor','none')

set(gcf,'renderer','painters')
exportgraphics(gcf,'gemb_profile_documentation_01.png',Resolution=300)
```

![](https://chadagreene.com/GEMB_figures/gemb_profile_documentation_01.png)

The synthetic climate forcing data simulate a location in Antarctica, where the surface is colder in June than in the final December timestep. 

You can now use either `ProfileOut` table as the input to a new `gemb` run that has been spun up. 

# Author Information
The Glacier Energy and Mass Balance (GEMB) was created by Alex Gardner, with contributions
from Nicole-Jeanne Schlegel and Chad Greene. Complete code and documentation are available
at [https://github.com/alex-s-gardner/GEMB](https://github.com/alex-s-gardner/GEMB). Please cite any use of GEMB as:

Gardner, A. S., Schlegel, N.-J., and Larour, E.: Glacier Energy and Mass Balance (GEMB): 
a model of firn processes for cryosphere research, Geosci. Model Dev., 16, 2277–2302, 
[https://doi.org/10.5194/gmd-16-2277-2023](https://doi.org/10.5194/gmd-16-2277-2023), 2023. 
